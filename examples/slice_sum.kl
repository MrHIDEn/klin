fn sum(xs: []i32): i32 {
    let mut i: i32 = 0
    let mut acc: i32 = 0
    while i < xs.len {
        acc = acc + xs[i]
        i = i + 1
    }
    return acc
}

fn main() {
    let buf: [4]i32 = [10, 20, 30, 40]
    printf("sum=%d\n", sum(buf[:]))
}
