# 086 — Language Server Protocol (`klin lsp`)

**Status:** ✅ MVP + cross-file goto + rename (merge [#165](https://github.com/klin-lang/klin/pull/165) when Actions recover)
**Depends on:** 002 (checker), 033 (`klin fmt`), 026 (preprocess)

## Goal

Thin Language Server in the **same** repo as the compiler — stdio JSON-RPC —
so editors can show diagnostics, format, hover, and go-to-definition without a
second parser.

## Progress

| Piece | Status |
|---|---|
| `klin lsp` stdio + `lsp_server` | ✅ |
| Document sync (full) + publish diagnostics | ✅ |
| `textDocument/formatting` → `formatSource` | ✅ |
| Hover + go-to-definition (same-file) | ✅ |
| Completion (keywords / scope / `.` members) | ✅ |
| `$fn` source maps | ✅ |
| Multi-error (check phase, per function) | ✅ |
| Cross-file goto (`loadProject` + open-buffer overlay) | ✅ |
| Rename (`textDocument/rename`) | ✅ |
| Source maps after SVD fluent rewrite | ✅ [091](091-lsp-svd-sourcemaps.md) |
| Lex/parse recovery / workspace index | → [092](092-lsp-parse-recovery.md) |
| TextMate / tree-sitter highlight | → [093](093-syntax-highlight.md) |
| IntelliJ / editor plugin | → [087](087-intellij-plugin.md) |
| Debug / DAP | → [088](088-dap-debug.md) |

## Done

- CLI: `klin lsp` (stdio)
- Diagnostics, format, hover, definition, completion
- [`SourceMap`](../lib/source_map.dart) after `$fn` expand
- Check-phase multi-error (`Checker.collectErrors`)
- Cross-file definition via [`loadProject`](../lib/project.dart) `sourceOverlay`
  (LSP open buffers win over disk)
- Rename via [`lib/rename.dart`](../lib/rename.dart)

## Follow-up issues

- [091](091-lsp-svd-sourcemaps.md) — SVD fluent source maps ✅
- [092](092-lsp-parse-recovery.md) — lex/parse recovery + index
- [093](093-syntax-highlight.md) — TextMate / tree-sitter (not LSP)
- [087](087-intellij-plugin.md) — IntelliJ plugin
- [088](088-dap-debug.md) — DAP / debug

## Docs

- CLI: [docs/06-cli.md](../docs/06-cli.md)

## Completion criteria

- [x] `klin lsp` stdio + diagnostics + format
- [x] Hover + definition (same file) + `$fn` maps
- [x] Completion
- [x] Multi-error check diagnostics
- [x] Cross-file goto with buffer overlay
- [x] Rename
- [x] Tests + `dart analyze` clean
