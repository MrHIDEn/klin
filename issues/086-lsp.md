# 086 — Language Server Protocol (`klin lsp`)

**Status:** 🔨 completion on branch `feat/086-lsp` (diagnostics/format/hover/definition ✅)
**Depends on:** 002 (checker), 033 (`klin fmt`)

## Goal

Thin Language Server in the **same** repo as the compiler — stdio JSON-RPC —
so editors can show diagnostics, format, hover, and go-to-definition without a
second parser.

## Progress

| Piece | Status |
|---|---|
| `klin lsp` stdio + `lsp_server` | ✅ |
| Document sync (full) + publish diagnostics | ✅ |
| `textDocument/formatting` → `formatSource` | ✅ |
| `lib/analyze.dart` + `Checker.requireMain` | ✅ |
| Hover + go-to-definition (same-file) | ✅ |
| Completion (keywords / scope / `.` members) | ✅ |
| Unit tests (`analyze` / `lsp` / `navigate` / `complete`) | ✅ |
| Docs CLI ([06](../docs/06-cli.md)) | ✅ |
| Rename | ❌ follow-up |
| Cross-file goto via `loadProject` + overlay | ❌ follow-up |
| Multi-error recovery / workspace index | ❌ follow-up |
| Source maps for `$fn` expand positions | ⏸ nav + completion disabled when skewed |
| IntelliJ / editor plugin | → [087](087-intellij-plugin.md) |
| Debug / DAP | → [088](088-dap-debug.md) |

## Done

- CLI: `klin lsp` (stdio)
- Document sync: `didOpen` / `didChange` / `didClose` (full text)
- Diagnostics from preprocess → lex → parse → check (one error at a time)
- `textDocument/formatting` → [`formatSource`](../lib/fmt.dart)
- `textDocument/hover` / `textDocument/definition` via `resolvedDef` on AST
  ([`lib/navigate.dart`](../lib/navigate.dart)); locals, fields, calls, methods
- `textDocument/completion` via [`lib/complete.dart`](../lib/complete.dart):
  keywords, prim types, top-level decls, locals; after `.` fields / methods /
  enum variants (uses last-good analysis when the buffer does not parse)
- Analysis facade: [`lib/analyze.dart`](../lib/analyze.dart)
- `Checker.check(..., {requireMain})` — CLI `true`; LSP `false`

## Out of scope (follow-up)

- Rename
- Cross-module definition (needs `loadProject` + buffer overlay)
- Multi-error recovery
- Incremental sync / workspace indexing
- IntelliJ plugin — [087](087-intellij-plugin.md)
- TextMate / tree-sitter highlight
- Debugger / DAP — [088](088-dap-debug.md)

## Docs

- CLI: [docs/06-cli.md](../docs/06-cli.md)
- FormatDocument: [033](033-gofmt-style.md) → this issue

## Completion criteria

- [x] `klin lsp` starts and speaks LSP over stdio
- [x] Diagnostics published on open/change; cleared on close
- [x] Formatting via `formatSource`; parse errors → empty edits (no crash)
- [x] Hover + definition for locals / fields / calls (same file)
- [x] Completion: keywords, scope names, `.` members (same file)
- [x] `test/analyze_test.dart` + `test/lsp_test.dart` + `test/navigate_test.dart` + `test/complete_test.dart`
- [x] `dart analyze` clean
