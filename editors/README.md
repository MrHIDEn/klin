# Editors

Packs that are **not** part of `klin lsp`:

| Path | What |
|---|---|
| [vscode/](vscode/) | TextMate grammar + VS Code / Cursor extension ([093](../issues/093-syntax-highlight.md)) |

IntelliJ LSP client + packaging → [087](../issues/087-intellij-plugin.md) (reuses the TextMate file under `vscode/syntaxes/`).
